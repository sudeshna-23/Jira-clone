{
  "name": "jira-clone",
  "version": "1.0.0",
  "private": true,

  "dependencies": {
    "json-server": "^1.0.0-beta.6"
  },

  "scripts": {
    "server": "json-server --watch data/Database.json --port 5000"
  }
}
